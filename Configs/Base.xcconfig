// 공통 빌드 설정.
// xcconfig 에서 // 는 주석이라 URL 의 // 는 $() 로 끊어 쓴다.

ZP_PROXY_BASE_URL = https:/$()/ai.zerolive.co.kr

// 비밀 값과 재정의는 저장소 루트의 Secrets.xcconfig 에 둔다.
// 파일이 없어도 빌드되도록 선택적 include 를 쓰고, 기본값보다 뒤에 둬서
// 같은 키가 있으면 Secrets 쪽이 이긴다.
#include? "../Secrets.xcconfig"
