{
	"name": "zeroplayer-landing",
	"version": "1.0.0",
	"private": true,
	"description": "zeroPlayer 랜딩 페이지 (Cloudflare Workers) — zeroplayer.zerolive.co.kr",
	"scripts": {
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"deploy": "wrangler deploy",
		"dry-run": "wrangler deploy --dry-run --outdir dist",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^5.20260921.1",
		"typescript": "^5.6.0",
		"wrangler": "^4.64.0"
	}
}
